---
meraki_api_key: "{{ meraki_key }}"
org_id: "{{ org_id }}"

SSID:
  - name: AE Guest
    number: 1
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    visible: false
    availableOnAllAps: true
    
  - name: AE Employees
    number: 2
    enabled: true
    authentication:
      auth_mode: psk
      encryption_mode: wpa
      psk: 
      wpa_encryption_mode: WPA2 only
    state: present
    visible: true
    availableOnAllAps: true
    visible: true
    availableOnAllAps: true
    

